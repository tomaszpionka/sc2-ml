# BLOCKER resolution log

**Date:** 2026-04-26
**Plan revision:** v3 (post-critique patch + sub-check 6 follow-up)
**Source critique:** `planning/current_plan.critique.md`
**Resolved-by:** planner-science (this revision). v3 awaits a targeted sub-check-6 verification by reviewer-adversarial; v2 BLOCKER-1..5 / WARNING-1..5 / NOTE-1..3 resolutions remain in force from prior verification.

| Finding | Action taken | Plan section changed | Remaining risk |
|---------|--------------|----------------------|----------------|
| BLOCKER-1 — qp_rm_1v1 mis-label on aoe2companion ID 18, propagated into Chapter 4 §4.1.3 / §4.2.3 | Q2 rewritten to *confirm* on-disk evidence and trace propagation; T05 sub-step 4.2 acknowledges on-disk evidence as starting point and adds fallback "default to quickplay/matchmaking on ambiguity, never default to ranked ladder"; T05 4.3 terminology ladder explicitly maps ID 18 / `qp_rm_1v1` to "third-party 1v1 Random Map quickplay/matchmaking records"; T05 step 5 (new) traces propagation from `data_quality_report_aoe2companion.md:29` to thesis sections; T11/T12/T13/T14 add explicit instructions to apply T05 terminology verbatim and remove "ladder rankingowy 1v1" wherever it traces to the mis-label; T10 risk register records the mis-label as concrete current state, not hypothetical | Problem Statement (item 3); Assumptions & unknowns (Unknown for ID 18); T05 Instructions 2/3/5/6, Verification, Read scope; T11 Instructions 3, Verification; T12 Instructions 7, Verification; T13 Instructions 8, Verification; T14 Instructions 6, Verification; T10 Instructions 1; Q2 rewrite; Gate Condition Thesis | External documentation may still be ambiguous; the fallback rule binds the executor to weakening rather than asserting, but external verification still depends on `@lookup` reachability. Manual review by T10's consolidated gate is the safety net. |
| BLOCKER-2 — LOCKED specs CROSS-02-00 v3 / CROSS-02-01 v1 require §7 amendment protocol that the plan did not invoke | T15 Instructions step 2 inserts explicit invocation of §7 protocol (classify; bump frontmatter version; amendment-log row; planner-science + reviewer-adversarial signoffs via T10's consolidated gate; same-commit discipline); T15 Verification adds spec frontmatter version-bump and amendment-log checks; T16 14A.2 explicitly notes v1→v2 major bump and applies §7 protocol; T16 14A.3 explicitly notes v3→v4 major bump and applies §7 protocol; T16 Verification extends with version/log/LOCKED-vs-modified consistency checks; File Manifest CROSS-02-00 / CROSS-02-01 rows note the §7 amendment classification; new "Specs" section under Gate Condition codifies the requirements; Assumption (D) records that T10 satisfies the planner-science + reviewer-adversarial co-signoff requirement | T15 Instructions 2, Verification, Read scope; T16 Instructions 2/3/6, Verification, File scope, Read scope; File Manifest spec rows; Gate Condition new "Specs" section; Assumptions & unknowns Assumption (D) | Some §7 reviewer-adversarial signoffs are now satisfied via T10's consolidated gate rather than per-spec dispatches. If T10 misses a §7-relevant concern, the spec change merges with weak adversarial review. Mitigation: T10 instructions explicitly list spec changes as gate items (i)–(iv). |
| BLOCKER-3 — T16 14A.1 data-dictionary manifest gap (file is generated; no manifest path connecting 14A.1 findings) | New conditional⁶ footnote defined; explicit File Manifest rows added for generated data-dictionary CSV/MD, the upstream notebook pair, and the ROADMAP/research_log/STEP_STATUS rows for Step 01_06_01; new manifest-bound HARD VERIFICATION RULE added to T07 and T16 Verification ("every modified file must have a corresponding manifest row"); T16 14A.1 Instructions explicitly point to conditional⁶ and require `notebook_regeneration_manifest.md` to record cause `T16 14A.1 finding` | T16 Instructions 1; T16 Verification (manifest-bound rule); T07 Verification (manifest-bound rule); File Manifest footnote ⁶ definition + new rows for data-dictionary files and Step 01_06_01 row updates; existing ROADMAP/research_log/STEP_STATUS rows extended with `/⁶` | Conditional⁶ overlaps with conditional¹/²/³ for the same files. Resolution chosen: union of trigger conditions, with `notebook_regeneration_manifest.md` recording the specific cause for each modified file. Risk: if multiple causes overlap, the executor must record both in the manifest, not pick one. The manifest format must accommodate this — verified by T07 / T16 Verification rule. |
| BLOCKER-4 — existing `thesis/pass2_evidence/` files unaccounted for in the manifest | T03 Instructions step 6 (new) adopts strategy B SUPPLEMENT: `claim_evidence_matrix.md` is a global cross-chapter index referencing existing crosswalks; existing crosswalks remain frozen per Pass-2 README convention; new Chapter 4 needs go into `sec_4_*_v2_crosswalk.md`; T03 Read scope adds the 5 existing pass2_evidence files (read-only; frozen); T11 Instructions step 5 directs creating v2 crosswalks if needed; T11 Read scope adds the v1 crosswalks (read-only); File Manifest adds rows for the 5 existing files (`Read-only (frozen — Pass-2 handoff convention)`) and conditional⁷ rows for v2 crosswalks and README updates; new conditional⁷ footnote defined; explanatory note added above the File Manifest table about the `Read-only (frozen)` action | T03 Instructions 6, Verification, File scope, Read scope; T11 Instructions 5, Verification, File scope, Read scope; File Manifest new note + new rows + conditional⁷ footnote; Out of scope adds prohibition on mutating v1 crosswalks; Gate Condition Evidence references claim_evidence_matrix and v1/v2 crosswalk relationship | Strategy B chosen (SUPPLEMENT). If T11 finds extensive Chapter 4 changes that would render v1 crosswalks misleading rather than merely incomplete, the strategy may need revisiting. Mitigation: T11 step 5 explicitly says "do not mutate v1"; revisiting requires escalation to user. |
| BLOCKER-5 — adversarial-cap exceedance (5 dispatches in original plan) | Collapsed to exactly 3 rounds: T02 (Round 1, plan critique, already produced), T10 (Round 2, consolidated mid-PR gate covering T05 provenance, T10 risk register, T15 + T16 14A.2 / 14A.3 spec changes), T19 (Round 3, final PR review); T05 step 6 REMOVES per-task adversarial dispatch and defers to T10; T15 step 6 REMOVES per-task adversarial dispatch and defers to T10; T16 14A.2 / 14A.3 satisfied by T10's consolidated gate; T19 instructions 5 explicitly note Round 3 of 3 and cap-waiver protocol if T19 finds substantial new issues; Assumption (D) added; T10 Objective renamed to include consolidated mid-PR adversarial gate | Assumptions & unknowns Assumption (D); T05 Instructions 6, Verification (defer dispatch); T10 Objective + Instructions 4, Verification, File scope, Read scope (consolidated gate); T15 Instructions 6, Verification (defer dispatch); T16 Verification (no per-task adversarial dispatch in 14A.2 / 14A.3); T19 Objective + Instructions 5, Verification (Round 3 / cap-waiver); Out of scope (4th round prohibited); Gate Condition Review (3 rounds explicit) | Single consolidated review at T10 covers many concerns at once; reviewer fatigue or scope-creep within T10 is possible. Mitigation: T10 Instructions 4 enumerates the four review topics explicitly so the reviewer can structure the review. |
| WARNING-1 / 6 — scope-explosion escape valve | T03 Instructions step 7 (new) and T06 Instructions step 5 (new): if T03 declares more than 10 Steps stale across all three datasets combined, halt before T06 and escalate to user with explicit (a)/(b)/(c) options; T03 Verification adds escape-valve check; T06 Verification adds wait-for-user-decision check; 18th risk added to T10 risk register | T03 Instructions 7, Verification, File scope; T06 Instructions 5, Verification; T10 Instructions 3 (18th risk); Assumptions & unknowns (escape valve note); Out of scope (escape valve clarifying note) | If T03's scope count is borderline (e.g. exactly 10), executor judgement is needed. Threshold is intentionally a numeric bound; the user may still elect a split below 10 if other factors warrant it. |
| WARNING-2 / 7 — notebook execution command | T07 Instructions step 1 cites the standard nbconvert command and the `sandbox/notebook_config.toml` `[execution] timeout_seconds` knob (default 600s); T07 Read scope adds `sandbox/notebook_config.toml`; T19 Instructions step 2 replaces "do not invent commands" with "use the nbconvert command from T07 step 1" | T07 Instructions 1, Read scope; T19 Instructions 2; Gate Condition Workflow (notebook execution clause) | None significant. Notebook timeout is bound to the existing repo config; if a notebook needs longer, the executor halts per T07 step 1. |
| WARNING-3 / 8 — branch-prefix verification | T01 Instructions step 5 (new) requires verifying CI workflows, pre-commit, PR template, and version-bump classification; T01 Verification records the four findings + version-bump classification; T01 Read scope adds the four files | T01 Instructions 5, Verification, Read scope; Assumptions & unknowns (Assumption B updated to reference T01 step 5) | Version-bump classification is a one-time choice; if the chosen classification is later disputed, follow-up PR governance applies. |
| WARNING-4 / 9 — bound aoestats Q3 | Q3 rewritten to anchor on `01_03_02_true_1v1_profile.md` and `01_04_01_data_cleaning.md` and require `@lookup` external verification, with explicit fallback "source-specific aggregation with semantic opacity" if source documentation is silent; T05 sub-step 4.1 Instructions extended with the same anchoring | Q3 rewrite; T05 Instructions 1, Read scope (adds the two artifacts); Assumptions & unknowns (Unknown for aoestats) | If neither artifact actually exists on disk yet (Phase 01 sub-step gaps), T05 4.1 will fall back to source documentation alone; flagged in the audit. |
| WARNING-5 / 10 — wildcard manifest wording | All `sandbox/<game>/<dataset>/**` and `src/rts_predict/games/<game>/datasets/<dataset>/reports/artifacts/**` rows updated with stronger wording: "only files explicitly declared stale in `notebook_regeneration_manifest.md` after T06; manifest is the authoritative bound; bare wildcard does not authorize touching arbitrary files"; matching HARD VERIFICATION RULE added to T07 / T16 Verification (also serving BLOCKER-3 fix) | File Manifest sandbox + artifacts rows; T07 Verification (manifest-bound rule); T16 Verification (manifest-bound rule); Gate Condition Workflow (manifest-bound clause) | Wording-only change; functional bound was already implicit in conditional² but is now explicit. |
| NOTE-1 / 11 — stale marking | T06 Instructions step 3 rewritten: stale marking lives in `notebook_regeneration_manifest.md`; do NOT rename artifacts with `_STALE` suffixes; downstream consumers consult the manifest before citing | T06 Instructions 3, Verification | None. The manifest already serves as the authoritative source of staleness; this just makes it the *only* source. |
| NOTE-2 / 12 — `is_cross_region_fragmented` retention threshold | T16 14A.5 Instructions rewritten: bind the retention threshold empirically to the WP-3 finding in `modeling_readiness_sc2egset.md` §5 or `01_05_*` cross-region analysis; record empirically derived threshold in `methodology_risk_register.md`; if no threshold can be empirically justified, keep as deferred decision rather than hard-coding magic number; T16 Read scope adds `modeling_readiness_sc2egset.md`; T10 risk register entry for cross-region fragmentation explicitly cites the empirically derived threshold or its deferred status | T16 Instructions 5, Verification, Read scope; T10 Instructions 1 (cross-region risk wording); Gate Condition Evidence (risk register includes empirical threshold or deferred status) | If empirical evidence on retention is absent or contradictory, the threshold defaults to deferred; modeling cannot proceed under unsafe magic-number assumptions but may also not proceed at all until Phase 02 surfaces the data. Acceptable per Invariant I7. |
| NOTE-3 / 13 — T19 regex tightening | T19 Instructions step 1 line 4 regex tightened from `master` to `\bbranch master\b` / `\bon master\b` / `\bmerged to master\b` / `\binto master\b`; T18 Verification regex similarly tightened; commentary added under T19 step 1 explaining false-positive avoidance | T18 Verification; T19 Instructions 1 (line 4), with commentary | If a thesis prose line uses an unusual phrase like "main branch" instead of "master", workflow leakage may slip through; mitigation: the cleanup_flag_ledger and T18 step 2 already cover internal workflow language broadly, so the regex is one of several lines of defence. |
| NOTE-4 — plan fidelity (strength acknowledgement) | No action required — strength acknowledgement only. | n/a | n/a |
| BLOCKER-1 sub-check 6 | Added Step 01_06_02 data-quality-report generator, artifact, ROADMAP, research_log, and STEP_STATUS entries to conditional⁶ manifest; extended conditional⁶ footnote and T16 14A.1 trigger to cover generated data-quality-report repair path | File Manifest conditional⁶; conditional⁶ footnote; T16 14A.1 | None beyond normal external verification of ID 18 semantics |
