# AoE2 phase progress — machine-readable status for Claude Code session start
# Updated after each phase gate is met. Claude Code reads this FIRST to know
# where the project stands without parsing the full roadmap.
#
# Statuses: not_started | in_progress | complete
# gate_date: ISO date when the gate condition was verified
# notes: brief context for the current state
#
# AoE2 has two independent datasets; Phase 0 runs once per dataset.

game: aoe2
dataset_roadmap: null  # No single dataset roadmap; see per-dataset reports
game_roadmap: src/rts_predict/aoe2/reports/ROADMAP.md
current_dataset: null  # Both datasets at same phase
current_phase: 0

phases:
  0:
    name: "Ingestion audit and raw table design"
    status: complete
    gate_date: "2026-04-07"
    notes: >-
      aoe2companion: 4,147 files, 4 raw tables (matches, ratings, leaderboard,
      profiles). 277M match rows, 58M rating rows. Reconciliation: DEGRADED
      (manifest has no per-file row counts). INVARIANTS.md written.
      aoestats: 172 match files, 171 player files (1 known failed download
      documented in manifest). 30.7M match rows, 107.6M player rows.
      Reconciliation: DEGRADED. INVARIANTS.md written. Both datasets: source
      audit PASS, schema profiled, smoke tested, CTAS ingested.
